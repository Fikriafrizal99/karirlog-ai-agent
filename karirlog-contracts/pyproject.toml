[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "karirlog-contracts"
version = "1.0.0"
description = "Shared KarirLog Job model and 15-column CSV contract"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
